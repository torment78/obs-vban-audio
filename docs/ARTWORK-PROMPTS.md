# VBAN Audio artwork editing prompts

Created with the built-in imagegen tool. The original banner graphics supplied
the edit targets. Renaming applies only to the main product title; the separate
`Windows x64 | OBS Studio` compatibility line remains unchanged. Each variant
was inspected before PNG and JPEG export.

## wide tagged

```text
Use case: text-localization. Make a surgical TEXT-ONLY edit to the FIRST image in your inputs: WIDE HORIZONTAL straight circuit banner WITH the VoiceMeeter credit. Ignore any other input images; they are NOT edit targets and MUST NOT be mixed into this image.
The product has been renamed from "OBS VBAN Audio" to "VBAN Audio". Change ONLY the LARGE WHITE MAIN TITLE from "OBS VBAN" / "AUDIO" to exactly "VBAN" / "AUDIO".
Delete "OBS" from that headline only, and reposition the existing VBAN lettering within its existing title area so the two-line VBAN / AUDIO title looks balanced. Use precisely the same existing font, white color, weight and letter height. Keep AUDIO in place. In landscape, left-align VBAN with AUDIO at the existing right-hand title block. In portrait, center VBAN directly above AUDIO.
ABSOLUTE INVARIANTS: Keep the original canvas dimensions, aspect ratio, full design, background, trace/wave geometry, all lighting and color, every logo element and its placement unchanged. No crop or resize. Keep every OTHER word exactly intact in its current position. In particular KEEP the compatibility line "Windows x64 | OBS Studio" exactly as it is: DO NOT remove OBS Studio there. Keep "8 inputs · 2 monitor returns" and "by ElkaSoft" exactly as they are.
If the target contains the small bottom-right VOICE MEETER / "VBAN protocol by VB-Audio" credit, retain it unmodified. If it has no such credit, do NOT add one. Preserve the precise left/center cyan double-arrow waveform icon and its two amber nodes: no redesign whatsoever.
This is the SAME graphic with only OBS removed from the large product title. No other changes. Output just this one edited image in the same orientation as its edit target.
```

## wide untagged

```text
Use case: text-localization. Make a surgical TEXT-ONLY edit to the FIRST image in your inputs: WIDE HORIZONTAL straight circuit banner WITHOUT the VoiceMeeter credit. Ignore any other input images; they are NOT edit targets and MUST NOT be mixed into this image.
The product has been renamed from "OBS VBAN Audio" to "VBAN Audio". Change ONLY the LARGE WHITE MAIN TITLE from "OBS VBAN" / "AUDIO" to exactly "VBAN" / "AUDIO".
Delete "OBS" from that headline only, and reposition the existing VBAN lettering within its existing title area so the two-line VBAN / AUDIO title looks balanced. Use precisely the same existing font, white color, weight and letter height. Keep AUDIO in place. In landscape, left-align VBAN with AUDIO at the existing right-hand title block. In portrait, center VBAN directly above AUDIO.
ABSOLUTE INVARIANTS: Keep the original canvas dimensions, aspect ratio, full design, background, trace/wave geometry, all lighting and color, every logo element and its placement unchanged. No crop or resize. Keep every OTHER word exactly intact in its current position. In particular KEEP the compatibility line "Windows x64 | OBS Studio" exactly as it is: DO NOT remove OBS Studio there. Keep "8 inputs · 2 monitor returns" and "by ElkaSoft" exactly as they are.
If the target contains the small bottom-right VOICE MEETER / "VBAN protocol by VB-Audio" credit, retain it unmodified. If it has no such credit, do NOT add one. Preserve the precise left/center cyan double-arrow waveform icon and its two amber nodes: no redesign whatsoever.
This is the SAME graphic with only OBS removed from the large product title. No other changes. Output just this one edited image in the same orientation as its edit target.
```

## vertical tagged

```text
Use case: text-localization. Make a surgical TEXT-ONLY edit to the FIRST image in your inputs: TALL VERTICAL straight circuit banner WITH the VoiceMeeter credit. Ignore any other input images; they are NOT edit targets and MUST NOT be mixed into this image.
The product has been renamed from "OBS VBAN Audio" to "VBAN Audio". Change ONLY the LARGE WHITE MAIN TITLE from "OBS VBAN" / "AUDIO" to exactly "VBAN" / "AUDIO".
Delete "OBS" from that headline only, and reposition the existing VBAN lettering within its existing title area so the two-line VBAN / AUDIO title looks balanced. Use precisely the same existing font, white color, weight and letter height. Keep AUDIO in place. In landscape, left-align VBAN with AUDIO at the existing right-hand title block. In portrait, center VBAN directly above AUDIO.
ABSOLUTE INVARIANTS: Keep the original canvas dimensions, aspect ratio, full design, background, trace/wave geometry, all lighting and color, every logo element and its placement unchanged. No crop or resize. Keep every OTHER word exactly intact in its current position. In particular KEEP the compatibility line "Windows x64 | OBS Studio" exactly as it is: DO NOT remove OBS Studio there. Keep "8 inputs · 2 monitor returns" and "by ElkaSoft" exactly as they are.
If the target contains the small bottom-right VOICE MEETER / "VBAN protocol by VB-Audio" credit, retain it unmodified. If it has no such credit, do NOT add one. Preserve the precise left/center cyan double-arrow waveform icon and its two amber nodes: no redesign whatsoever.
This is the SAME graphic with only OBS removed from the large product title. No other changes. Output just this one edited image in the same orientation as its edit target.
```

## vertical untagged

```text
Use case: text-localization. Make a surgical TEXT-ONLY edit to the FIRST image in your inputs: TALL VERTICAL straight circuit banner WITHOUT the VoiceMeeter credit. Ignore any other input images; they are NOT edit targets and MUST NOT be mixed into this image.
The product has been renamed from "OBS VBAN Audio" to "VBAN Audio". Change ONLY the LARGE WHITE MAIN TITLE from "OBS VBAN" / "AUDIO" to exactly "VBAN" / "AUDIO".
Delete "OBS" from that headline only, and reposition the existing VBAN lettering within its existing title area so the two-line VBAN / AUDIO title looks balanced. Use precisely the same existing font, white color, weight and letter height. Keep AUDIO in place. In landscape, left-align VBAN with AUDIO at the existing right-hand title block. In portrait, center VBAN directly above AUDIO.
ABSOLUTE INVARIANTS: Keep the original canvas dimensions, aspect ratio, full design, background, trace/wave geometry, all lighting and color, every logo element and its placement unchanged. No crop or resize. Keep every OTHER word exactly intact in its current position. In particular KEEP the compatibility line "Windows x64 | OBS Studio" exactly as it is: DO NOT remove OBS Studio there. Keep "8 inputs · 2 monitor returns" and "by ElkaSoft" exactly as they are.
If the target contains the small bottom-right VOICE MEETER / "VBAN protocol by VB-Audio" credit, retain it unmodified. If it has no such credit, do NOT add one. Preserve the precise left/center cyan double-arrow waveform icon and its two amber nodes: no redesign whatsoever.
This is the SAME graphic with only OBS removed from the large product title. No other changes. Output just this one edited image in the same orientation as its edit target.
```

## waves tagged

```text
Use case: text-localization. Make a surgical TEXT-ONLY edit to the ONLY image in your inputs: WIDE HORIZONTAL waves banner WITH the VoiceMeeter credit. Ignore any other input images; they are NOT edit targets and MUST NOT be mixed into this image.
The product has been renamed from "OBS VBAN Audio" to "VBAN Audio". Change ONLY the LARGE WHITE MAIN TITLE from "OBS VBAN" / "AUDIO" to exactly "VBAN" / "AUDIO".
Delete "OBS" from that headline only, and reposition the existing VBAN lettering within its existing title area so the two-line VBAN / AUDIO title looks balanced. Use precisely the same existing font, white color, weight and letter height. Keep AUDIO in place. In landscape, left-align VBAN with AUDIO at the existing right-hand title block. In portrait, center VBAN directly above AUDIO.
ABSOLUTE INVARIANTS: Keep the original canvas dimensions, aspect ratio, full design, background, trace/wave geometry, all lighting and color, every logo element and its placement unchanged. No crop or resize. Keep every OTHER word exactly intact in its current position. In particular KEEP the compatibility line "Windows x64 | OBS Studio" exactly as it is: DO NOT remove OBS Studio there. Keep "8 inputs · 2 monitor returns" and "by ElkaSoft" exactly as they are.
If the target contains the small bottom-right VOICE MEETER / "VBAN protocol by VB-Audio" credit, retain it unmodified. If it has no such credit, do NOT add one. Preserve the precise left/center cyan double-arrow waveform icon and its two amber nodes: no redesign whatsoever.
This is the SAME graphic with only OBS removed from the large product title. No other changes. Output just this one edited image in the same orientation as its edit target.
```

## waves wide untagged

```text
Use case: precise-object-edit. Edit the ONLY input image: the newly renamed WIDE waves-and-circuit VBAN AUDIO banner. Remove ONLY the small bottom-right VOICE MEETER badge and the small caption "VBAN protocol by VB-Audio" underneath it. Fill ONLY that small removed area naturally with the existing dark navy background. Everything else remains visually identical: exact 1774 × 887 landscape canvas, left cyan arrow-ring waveform and amber nodes, blue/amber waves, straight edge circuit lines, all typography, title VBAN / AUDIO, subtitle "8 inputs · 2 monitor returns", "Windows x64 | OBS Studio" compatibility line, and "by ElkaSoft". Keep every remaining element in its exact current place and at the exact current scale. No other changes.
```

## waves vertical tagged

```text
Use case: compositing / portrait layout adaptation.
Input image 1 is the renamed VERTICAL STRAIGHT-CIRCUIT VBAN AUDIO banner and is ONLY a portrait layout reference. Input image 2 is the renamed WIDE WAVES-AND-CIRCUIT VBAN AUDIO banner and is the design/style target.
Make the VERTICAL portrait companion of the WAVES banner, at the same tall approximately 9:16 proportions as image 1 (941 × 1672 preferred). Match its generous vertical layout: title at the top, unchanged original VBAN Audio icon centered in the middle, subtitle and compatibility below, brand/footer credits at the bottom.
Preserve the exact original logo design from image 2: cyan two-arrow ring, cyan waveform bars and two amber endpoint nodes in cyan-rimmed circles. All colors, shapes and proportions unchanged; scale uniformly. The large white title is exactly "VBAN" then "AUDIO", never OBS before VBAN. Same bold font and white color.
Preserve all other text verbatim: cyan "8 inputs · 2 monitor returns"; muted grey "Windows x64 | OBS Studio"; italic "by ElkaSoft" with orange Elka and cyan Soft; small bottom-right credit with white VOICE in a red box and white MEETER beside it, grey "VBAN protocol by VB-Audio" below.
Background MUST use the target WAVES-AND-CIRCUIT design, not all straight traces: dark navy atmospheric depth, blue/cyan and warm amber flowing fine light waves across the lower portion behind the logo, and sparse straight angled cyan circuit accents at the edges, small luminous nodes, tasteful bloom and near/far depth. Leave quiet dark space behind lettering. Preserve the style and branding of image 2, merely arranged as a tall portrait companion. No added words or objects. No mockup.
```

## waves vertical untagged

```text
Use case: precise-object-edit. Edit ONLY the most recent tall portrait VBAN AUDIO banner with flowing cyan/amber waves and sparse straight edge circuit traces. Remove ONLY the small VOICE MEETER badge and "VBAN protocol by VB-Audio" caption in the bottom-right corner. Cleanly reconstruct the same dark navy background inside that small removed area. Keep absolutely every other element identical: same portrait canvas and dimensions, logo design and placement, waveform bars, cyan ring arrows, two amber endpoint nodes, every wave and circuit trace, lighting, every word and font and position. Retain "VBAN" / "AUDIO", "8 inputs · 2 monitor returns", "Windows x64 | OBS Studio", and "by ElkaSoft". No crop, redesign, re-layout or new text. Output the exact matching portrait version WITHOUT the VoiceMeeter credit.
```
